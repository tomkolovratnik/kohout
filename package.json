{
  "name": "kohout",
  "version": "1.0.2",
  "description": "Personal ticket aggregation tool for Jira and Azure DevOps",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "bin": {
    "kohout": "bin/kohout.js"
  },
  "files": [
    "bin/",
    "server/dist/",
    "server/package.json",
    "client/dist/"
  ],
  "dependencies": {
    "sql.js-fts5": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^5.1.0",
    "zod": "^3.24.0"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "prepare": "npm run build",
    "start": "node server/dist/index.js",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
